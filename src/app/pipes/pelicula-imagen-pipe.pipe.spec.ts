import { PeliculaImagenPipePipe } from './pelicula-imagen-pipe.pipe';

describe('PeliculaImagenPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaImagenPipePipe();
    expect(pipe).toBeTruthy();
  });
});
